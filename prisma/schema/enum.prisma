enum UserRole {
  admin
  user
}

enum Status {
  pending
  approved
  rejected
}

enum PaymentStatus {
  pending
  completed
  failed
  refunded
}

enum CreditDirection {
  credit
  debit
}

enum CreditReason {
  purchase
  extra_message
  gift
}

enum ChatMessageType {
  text
  gift
}

enum GiftTransactionStatus {
  completed
  refunded
}
